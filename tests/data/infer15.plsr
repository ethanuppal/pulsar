pure func square(value: Int) -> Int {
    return value * value
}

func sum_squares(input: Int[4]) -> Int {
    let squares = map<4>(square, input)
    return 0
    let a = 3
    return 0
}

pure func square2(value: Int) -> Int {
    return value * value
}

func sum_squares2(input: Int[4]) -> Int {
    let squares = map<4>(square2, input)
    return 0
    return 0
}
