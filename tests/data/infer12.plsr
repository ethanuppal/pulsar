func main(input: [[Int; 64]; 64], kernel: [[Int; 3]; 3]) -> (output: [[Int; 62]; 62]) {
    for row in 1 ..< 63 {
        for col in 1 ..< 63 {
            output[row][col] = input[row][col]
        }
    }
}
